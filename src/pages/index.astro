---

---

<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Minjoo's Home</title>
	</head>
	<body>
		<header class="site-header">
			<a class="brand" href="/">Minjoo</a>
			<nav aria-label="Primary navigation">
				<a href="#about">About</a>
				<a href="#projects">Projects</a>
				<a href="#posts">Posts</a>
			</nav>
		</header>

		<main>
			<section class="hero" aria-labelledby="hero-title">
				<p class="eyebrow">Personal Homepage</p>
				<h1 id="hero-title">안녕하세요, Minjoo입니다.</h1>
				<p class="intro">배우고 만들고 기록하는 개인 홈페이지를 준비하고 있습니다.</p>
				<p class="summary">
					이곳은 앞으로 소개, 프로젝트, 글을 차근차근 모아둘 작은 작업실입니다.
					지금은 첫 Home 페이지부터 시작합니다.
				</p>
			</section>

			<section class="sections" aria-label="Homepage sections">
				<a class="section-link" id="about" href="#about">
					<span>About</span>
					<p>나를 소개하는 공간을 만들 예정입니다.</p>
				</a>
				<a class="section-link" id="projects" href="#projects">
					<span>Projects</span>
					<p>진행한 작업과 배운 내용을 정리할 예정입니다.</p>
				</a>
				<a class="section-link" id="posts" href="#posts">
					<span>Posts</span>
					<p>짧은 글과 기록을 쌓아갈 예정입니다.</p>
				</a>
			</section>
		</main>
	</body>
</html>

<style>
	:root {
		color: #191919;
		background: #ffffff;
		font-family:
			Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
			sans-serif;
	}

	* {
		box-sizing: border-box;
	}

	body {
		margin: 0;
		background: #ffffff;
		color: #191919;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	.site-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 24px;
		max-width: 1080px;
		margin: 0 auto;
		padding: 32px 24px;
	}

	.brand {
		font-size: 18px;
		font-weight: 700;
	}

	nav {
		display: flex;
		gap: 20px;
		color: #666666;
		font-size: 15px;
	}

	nav a:hover,
	.section-link:hover span {
		color: #111111;
	}

	main {
		max-width: 1080px;
		margin: 0 auto;
		padding: 72px 24px 96px;
	}

	.hero {
		max-width: 780px;
		padding: 64px 0 88px;
	}

	.eyebrow {
		margin: 0 0 20px;
		color: #767676;
		font-size: 14px;
		font-weight: 700;
		letter-spacing: 0;
		text-transform: uppercase;
	}

	h1 {
		margin: 0;
		font-size: clamp(48px, 8vw, 92px);
		line-height: 1.04;
		letter-spacing: 0;
		word-break: keep-all;
	}

	.intro {
		max-width: 720px;
		margin: 28px 0 0;
		font-size: clamp(22px, 3vw, 32px);
		line-height: 1.4;
		color: #333333;
		word-break: keep-all;
	}

	.summary {
		max-width: 640px;
		margin: 24px 0 0;
		color: #666666;
		font-size: 18px;
		line-height: 1.8;
		word-break: keep-all;
	}

	.sections {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 16px;
		border-top: 1px solid #e7e7e7;
		padding-top: 28px;
	}

	.section-link {
		min-height: 168px;
		padding: 24px;
		border: 1px solid #e7e7e7;
		border-radius: 8px;
	}

	.section-link span {
		display: block;
		font-size: 22px;
		font-weight: 700;
	}

	.section-link p {
		margin: 18px 0 0;
		color: #666666;
		font-size: 16px;
		line-height: 1.7;
	}

	@media (max-width: 720px) {
		.site-header {
			align-items: flex-start;
			flex-direction: column;
			padding-top: 24px;
		}

		nav {
			flex-wrap: wrap;
		}

		main {
			padding-top: 40px;
		}

		.hero {
			padding: 40px 0 64px;
		}

		.sections {
			grid-template-columns: 1fr;
		}
	}
</style>
